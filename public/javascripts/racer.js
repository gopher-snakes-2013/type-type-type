document.addEventListener("keypress",whatKey,false)

function whatKey(e){
	(e.keyCode)
}

var text = document.getElementById("text")
//tester = document.querySelectorAll("#test.c_test").length;


// for(i=0; i<; i++){}



//write a function that will compare keycode against
//keycode of letter in html